version: '3'
services:
  web:
    build: .
    ports:
      - "5000:5000"
  db:
    image: postgres:latest
    environment:
      POSTGRES_DB: agriculture
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
  ml-service:
    build: ./ml-service
    ports:
      - "5001:5000"